# AGENTS.md

현재 작업 폴더 `AI 업무 개선/`가 실제 프로젝트 루트다. `AGENTS.md`, `ANALYSIS_METHOD.md`, `VALIDATION.md`, `README.md`는 모두 루트에 있다. `scripts/`, `tests/`, `data/`, `_out/`도 이 루트 아래에 있으며, 같은 이름의 하위 프로젝트 폴더나 `docs/`에서 지침을 찾지 않는다.

## 작업 시작 전 필수 확인

- 새 작업을 시작할 때, 답변 작성·웹 검색·데이터 조회·계산·파일 수정 전에 아래 'GitHub 최신 지침 확인'을 수행하고, 해당 버전의 AGENTS.md와 아래 '필요한 문서' 표의 해당 문서를 먼저 읽고 적용한다. 지침을 찾고 읽기 위한 탐색과 GitHub 조회는 먼저 수행할 수 있다.
- 사용자가 지침 확인을 요청하지 않아도 수행한다. 일반적인 질의응답 형식의 요청도 이 프로젝트의 업무라면 예외로 두지 않는다.
- 같은 작업을 이어가는 동안 이미 읽었고 변경되지 않은 문서는 다시 읽지 않아도 되지만, 지침 적용은 계속 유지한다. 새 작업이거나 지침이 변경되었으면 다시 확인한다.
- 문서를 읽었다는 사실과 그 문서의 분석·검증 절차를 완료했다는 사실을 구분한다. 누락을 발견하면 충족하지 못한 항목을 명시하고 필요한 단계부터 바로잡는다.

### GitHub 최신 지침 확인

지침의 기준 저장소는 [jeongpal0405-wq/Globalresearch](https://github.com/jeongpal0405-wq/Globalresearch), 기준 브랜치는 `main`이다. 로컬 AGENTS.md는 Codex가 이 확인 절차를 시작하는 진입점이다. 사용자가 별도로 GitHub 조회를 요청하지 않아도 아래 절차를 수행한다.

1. 연결된 GitHub 플러그인의 `github_fetch`로 `https://api.github.com/repos/jeongpal0405-wq/Globalresearch/git/ref/heads/main`을 조회한다. 응답의 `object.sha`를 이번 작업의 지침 커밋으로 기록한다. 로컬 `origin/main`이나 이전 대화의 커밋을 최신판이라고 간주하지 않는다.
2. `github_fetch_file`에 `repository_full_name="jeongpal0405-wq/Globalresearch"`, `path="AGENTS.md"`, `ref=확인한 전체 커밋 SHA`를 지정해 원문 전체를 읽는다. 이어서 작업에 필요한 `ANALYSIS_METHOD.md`, `VALIDATION.md`, `README.md`도 반드시 같은 SHA로 읽는다. 지침 확인을 위한 도구 탐색은 허용한다.
3. GitHub에서 읽은 해당 버전의 지침을 이번 작업에 적용한다. 로컬 사본과 다르면 GitHub판을 기준으로 하되, 시스템·개발자 지침과 사용자가 현재 작업에서 명시한 요청·확정 기준을 우선한다. 사용자가 로컬에서 편집 중인 지침은 작업본으로 보존하며 GitHub판으로 덮어쓰거나 폐기하지 않는다.
4. 작업 시작 안내에 `GitHub 지침 확인: main@짧은 SHA`와 읽은 문서명을 짧게 남긴다. 실제 원문을 읽기 전에는 확인 완료라고 쓰지 않는다. 새 작업이거나 사용자가 지침 갱신을 알리면 다시 조회하고, 같은 작업의 단순 후속 대화에서는 확인한 버전을 유지한다. 이미 원격 AGENTS.md를 읽은 과정에서 이 절차를 다시 만났다고 재귀적으로 재조회하지 않는다.
5. 플러그인 연결·권한·네트워크 문제나 문서 누락으로 최신판을 확인하지 못하면 원인과 못 읽은 파일을 알린다. 구버전을 최신판처럼 적용하지 않으며, 지침에 의존하는 분석은 보류한다. 연결 복구·지침 확인 작업은 계속할 수 있고, 사용자가 로컬 지침 사용을 명시하면 그 버전과 한계를 표시해 진행한다.

이 절차는 Codex가 지침에 따라 GitHub를 조회하는 방식이다. 별도 프로그램이나 `SessionStart` 훅을 설치한 것은 아니다. 지침 조회 자체는 로컬 파일 수정·전체 저장소 동기화·자동 커밋·자동 업로드를 수행하지 않는다.

## 프로젝트와 역할

애널리스트의 시나리오에 해당하는 과거 사례를 찾아 원하는 자산의 이후 성과를 분석한다.
분석 대상에는 경제지표·시장 수치 조건뿐 아니라 정치·정책·지정학·금융 사건과 이들의 복합 조건도 포함한다. 사건 정의·시점·가격 자료의 확인 가능 범위에 따라 분석하며, 모든 사건을 자동 수집·판정할 수 있다고 가정하지 않는다.
Codex는 조건 확인, 지정 출처 조회, 데이터 검증, 계산 절차 구성, 자체 해석, 보고서 작성까지 담당한다. 수치 계산은 Python 코드로 수행한다.

## 필요한 문서

| 작업 | 읽을 문서 |
|---|---|
| 모든 작업 | 이 문서: 핵심 운영 규칙 |
| 조건 구조화·데이터 수집·계산·보고서 작성 | [상세 분석 방법](ANALYSIS_METHOD.md) |
| 구현 점검·결과 검증·완료 판단 | [검증 사례와 진행 상태](VALIDATION.md) |
| 프로젝트 구조·개발 환경 확인 | [README](README.md) |

상세 규칙은 담당 문서 한 곳에서 관리하고, 변경하면 연결된 검증 사례도 갱신한다. CLAUDE.md와 백업은 과거 기록이며 현재 분석 지침으로 적용하지 않는다. 사용자가 새로 확정한 기준은 해당 문서에 반영한다.

## 작업 순서

1. 질문에서 조건·대상 자산·분석 기간을 정리한다. 모호한 조건과 미확정인 조건 판정 시점은 사용자에게 질문한다.
2. 답변으로 확정된 조건을 요약하고, 필요한 데이터와 확보 가능 범위를 확인한다.
3. 아래 출처 규칙에 따라 자료를 확보하고 원본·출처·발표 당시 값 여부를 검증한다.
4. 전체 대상 이력에 조건을 적용하고 사례별·기간별 성과와 비교 기준을 코드로 계산한다.
5. 계산 결과와 해석의 근거를 확인하고 보고서를 작성한다. 수행하지 못한 검증과 남은 제약을 함께 알린다.

## 사용자에게 확인할 사항

- 조건의 의미가 모호하면 가능한 해석과 차이를 짧게 설명하고 답변을 받는다. 응답이 없다고 임의의 기준을 적용하지 않는다.
- 조건 판정 시점, 조건 간 시간 관계, 반복 발생 처리, 기산일은 미확정이면 질문한다. 이미 명시·확정한 내용을 다시 묻지 않는다.
- 모호한 조건에 의존하는 표본 선정·계산은 답변 전까지 보류한다.
- 휴장·장 마감 후 발표·해외 시차, 발표일이 없는 시장 조건의 기준일은 사용자에게 확인한다.
- 발표일 종가·거래일 기준은 확정된 규칙이다. 가격 조정·배당·통화 기준 등 결과를 바꾸는 미확정 사항은 별도로 확인한다.
- 데이터나 사례가 부족하다는 이유로 조건·자산·출처를 임의로 변경하거나 완화하지 않는다.

## 출처와 데이터

- 경제지표의 컨센서스가 필요한 데이터는 Investing.com을 사용한다.
- 경제지표·금리·자산가격 등 정량자료 중 컨센서스가 필요하지 않은 데이터는 FRED API를 최우선으로 확인하고, 요건에 맞는 자료를 확보하지 못하면 Investing.com을 사용한다. 전환 이유를 기록한다.
- Investing.com은 브라우저로 페이지를 열어 표시된 데이터를 직접 확인한다. 직접 HTTP 요청·비공식 API 스크래핑으로 대체하지 않는다.
- 정치·정책·지정학·금융 사건의 사실·결과는 해당 국회·법원·정부·중앙은행·거래소 등 담당 기관의 공식 기록을 우선한다. 국내 뉴스는 연합뉴스, 해외 뉴스는 Reuters·AP의 접근 가능한 원문을 우선 후보로 삼아 보완·교차 확인한다. 세부 출처와 절차는 [상세 분석 방법](ANALYSIS_METHOD.md)의 '정치·경제 사건과 뉴스 자료'를 따른다.
- 사건 발생 시각·공식 공개 시각·기사 최초 게시 시각·기사 수정 시각을 구분한다. 검색 결과 중 가장 오래된 기사를 시장의 최초 인지 시점으로 단정하지 않는다. 원문·시점·결과를 확인하지 못하면 해당 항목을 미확인으로 남긴다.
- 지정한 출처에서 필요한 자료를 확보하지 못하면 부족한 항목을 알린다. 다른 출처나 비슷한 지표를 임의로 사용하지 않는다.
- 경제지표는 항상 해당 발표 당시 값을 사용한다. 최신 수정치로 대체하지 않고, 당시 값인지 확인할 수 없으면 판정 불가로 표시한다.
- 표본은 요청 기간의 전체 발표 이력·전체 대상 거래일 또는 정의된 사건의 전체 목록에서 만든다. 기사·검색 결과에 등장한 날짜만 골라 전체 표본으로 취급하지 않는다. 사건 목록의 완전성을 확인하지 못하면 확보 범위와 누락 가능성을 표시한다.
- 원본을 보존하고 정제는 사본에서 한다. 출처 주소·시리즈 식별자·수집일·수집 범위·당시 값의 근거를 기록한다.
- 결측을 0으로 채우지 않는다. 사례 없음·자료 부족·관찰기간 미완료·수집 실패를 구분한다.
- 기존 yfinance·BLS 직접 조회 코드와 캐시는 새 출처 규칙을 충족한 것으로 간주하지 않는다.

## 계산·해석·산출물

- 기본 수익률은 발표일 종가를 0일로 삼은 이후 1·5·20·60거래일 성과다. 발표일 이전 종가에서 시작하는 등락을 포함하지 않는다.
- 사용자에게 확인한 시간 기준과 당시 공개된 정보로 조건을 판정한다. 사후에 확인된 조건을 기산일에 알고 있었던 것처럼 다루지 않는다.
- 원래 정밀도로 조건을 판정·계산하고, 표시 단계에서만 소수점 첫째 자리까지 반올림한다. 날짜·사례 수·식별자는 본래 형식을 유지한다.
- 조건 단계별 사례 수, 기간별 유효 사례 수, 동일 기준의 무조건부 베이스라인, 데이터 한계를 함께 보여준다.
- 공식 기록·기사에서 확인한 사건 사실과 시점은 출처를 붙여 기록·요약할 수 있다. 매체·전문가의 시장 해석·전망·투자의견을 인용하거나 요약해 자체 분석을 대신하지 않는다. 이후 성과는 가격 자료로 직접 계산하고 관찰과 해석을 구분한다.
- 모든 조건을 설명하되 원인을 뒷받침할 근거가 없으면 "관측 데이터만으로 원인 특정 불가"라고 적는다. 전망·목표가·투자의견은 작성하지 않는다.
- 기본 산출물은 A4 세로 PDF다. 사례 목록·수익률·최대낙폭·차트·자체 해석을 담고 텍스트와 렌더링을 검증한다.

## 변경과 완료 판단

- 요청 범위에 필요한 변경만 하고 기존 코드 스타일과 사용자 변경을 보존한다.
- 다단계 작업은 단계와 검증 방법을 먼저 알린다. 사용 중인 실행 환경과 의존성을 확인한다.
- 분석 규칙이 문서에 있다는 이유로 구현 완료라고 보고하지 않는다. 실제 실행한 검증과 미실행 항목을 구분한다.
- 계산 로직을 변경하면 관련 검증 사례를 실행한다. 데이터 수집·보고서까지 확인하지 않았다면 전체 분석 검증 완료라고 하지 않는다.
- API 키는 .env에 보관하고 코드·로그·Git에 노출하지 않는다. 수집 데이터·캐시·생성 보고서는 현재 .gitignore의 제외 범위를 유지한다.
