# CLAUDE.md

## 0. 프로젝트

**과거 유사국면 · 이벤트 스터디 도구.**
자연어 질문 → 조건에 맞는 과거 사례 탐색 → 이벤트 이후 지수 · 업종 · 종목 성과 통계 자동 산출.

> 예: "미국 CPI가 예상치를 0.2%p 이상 상회하고 10년물 금리가 10bp 이상 오른 날, 나스닥과 반도체지수의 이후 성과는?"

산출물: 조건 부합일 목록 + 당일/1일/5일/20일/60일 포워드 수익률 · 최대낙폭 + 사례별 차트 + 기사 근거 코멘트.

---

## 1. 코딩 행동 지침

> 속도보다 신중함으로 기운다. 사소한 작업엔 판단력을 발휘할 것.

**1.1 코딩 전에 생각하기** — 가정하지 말 것. 혼란을 숨기지 말 것.
- 가정을 명시한다. 불확실하면 묻는다.
- 해석이 여러 개면 모두 제시한다. 조용히 하나를 고르지 않는다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반대한다.
- 불명확하면 멈춘다. 뭐가 헷갈리는지 이름 붙이고 묻는다.

**1.2 단순함 우선** — 문제를 푸는 최소한의 코드.
- 요청받지 않은 기능 · 추상화 · "유연성" 금지.
- 불가능한 시나리오에 대한 에러 핸들링 금지.
- 200줄을 썼는데 50줄이면 될 것 같으면 다시 쓴다.

**1.3 외과적 변경** — 꼭 필요한 것만 건드린다.
- 인접 코드 · 주석 · 포매팅을 "개선"하지 않는다. 기존 스타일을 따른다.
- 무관한 데드 코드는 **언급만** 한다. 지우지 않는다.
- 단, 내 변경 때문에 안 쓰이게 된 import · 변수는 제거한다.
- 테스트: 변경된 모든 줄이 사용자 요청으로 직접 추적되어야 한다.

**1.4 목표 주도 실행** — 성공 기준을 정의하고 검증될 때까지 반복한다.
- "버그 수정" → "버그를 재현하는 테스트를 쓰고 통과시킨다"
- 다단계 작업은 `단계 → 검증 방법` 형태로 계획을 먼저 진술한다.

---

## 2. 도메인 불변 규칙 — 재논의 금지

### 규칙 A. 표본은 **캘린더 전체**에서 온다. 검색어가 고르지 않는다. ★핵심★

```
❌ "CPI beat expectations" 검색 → 나온 날짜들을 분석 대상으로 삼음
✅ Investing.com 지표별 히스토리 표를 통째로 가져옴 (상회·부합·하회 전부)
   → surprise = actual − forecast 직접 계산 → 임계값 필터
```

**왜.** 뉴스는 시장이 크게 반응한 날을 크게 보도한다. 보도 강도가 **우리가 측정하려는 결과(시장 충격)와 상관**되므로, 검색으로 표본을 고르면 "상회했지만 시장이 무시한 날"이 체계적으로 빠지고 이벤트 효과가 **과대추정**된다. 또 부합일·하회일이 없으면 베이스라인이 없어 "상승확률 58%"가 높은지 낮은지 말할 수 없다.

실무적으로도, `"≥0.2%p 상회"`를 뜻하는 검색어는 **존재하지 않는다.** 기사는 "0.4%, 예상 0.3%"라고 쓰지 "0.1%p 상회"라고 쓰지 않는다. 서프라이즈 폭은 **계산으로만** 나온다.

### 규칙 B. 세부값은 finalist 날짜에만. 해석은 숫자에서 도출한다 ★2026-09-08 개정★

**경계선.** 조건 판정(규칙 A)이 끝나 finalist가 확정된 뒤부터가 규칙 B 영역이다.

```
[규칙 A · 캘린더 전체]  data/events/*.csv 98개월 → screen.py → finalist n건
━━━━━━━━━━━━━━━━━━━━━━━ 경계 ━━━━━━━━━━━━━━━━━━━━━━━
[규칙 B · finalist만]   fetch_bls.py → data/facts/  → 해석 → data/anatomy/
```

**지표가 조건에 쓰이면 전체 히스토리가 필요하고(규칙 A), 해부에만 쓰이면 finalist 날짜만 받는다.**
나중에 `ISM 고용지수 < 50` 같은 걸 조건으로 올리면, 그때 그 지표의 99행을 새로 확보해야 한다.

**후보군 생성에는 절대 쓰지 않는다.** 세부값을 보고 표본을 고르면 규칙 A가 무너진다.

#### 해석은 우리가 쓴다. 기사에서 옮기지 않는다

이전 규격은 코멘트 출처를 1차 보도 매체로 한정했다. **폐기한다.** 이유는 둘이다.

1. 페이월·봇차단으로 실제로 못 열었다 — 지난 실행에 `"코멘트 확보 실패 (크롤러 차단)"`가 남았고, 그게 보고서가 허전한 직접 원인이었다.
2. 기사 인용은 검증이 안 된다. **"이 문장을 숫자에서 다시 만들 수 있나"는 기계가 판정할 수 있다.**

**쓸 수 있는 해석 다섯 가지** — 전부 우리가 가진 파일에서 도출된다.

| | 근거 |
|---|---|
| 구조 — 무엇이 헤드라인을 만들었나 | `data/facts/*.json` 항목별 값 |
| 시계열 맥락 — 직전 흐름과 뭐가 다른가 | `data/events/*.csv` 99행 |
| 분포 위치 — 이 값이 얼마나 드문가 | `data/events/*.csv` 99행 |
| 베이스라인 대비 | `_out/results.json` |
| 부호 · 지속성 | `_out/results.json` |

**쓸 수 없는 것**: 일회성 요인의 *원인*(정책·사건), 당일 시장이 움직인 *이유*. 숫자에서 안 나온다.
→ **비워 두거나 그 문장을 쓰지 않는다. 추측으로 메우지 않는다.**

- **전망 · 목표가 · 투자의견은 쓰지 않는다.** 우리 보고서는 과거 사례 기술이지 전망물이 아니다.
- **조건에 쓰인 지표는 전부 해부 대상이다.** 기산일 지표만 파고 끝내지 않는다 (6.2).
- 자료를 못 구하면 **그 표만** "확보 실패"로 두고 나머지는 채운다.

### 규칙 C. 시장 변수 조건은 시계열에서 조인
"10년물 +10bp", VIX, 달러인덱스 같은 조건은 FRED · 가격 데이터에서 **날짜 기준 조인**. 뉴스로 찾지 않는다.

### 규칙 D. 복합조건은 퍼널
`A 만족일 → 그중 B 만족 → 그중 C 만족`. 정상적인 방식이다. 단 **A가 완전해야** 의미가 있다 (규칙 A). A가 편향되면 뒤에서 뭘 해도 안 고쳐진다.

---

## 3. 데이터 소스

| 데이터 | 소스 | 방식 |
|---|---|---|
| **지표 발표 이력 (날짜 · actual · forecast · previous)** | **Investing.com 경제캘린더** — 지표별 히스토리 페이지 | **수동 복사** → CSV 고정 |
| 거시 시계열 (금리, VIX 등) | FRED | API (키는 사용자 제공, `.env`, 커밋 금지) |
| 지수 · ETF · 종목 (미국 · 한국 공통) | yfinance | 라이브러리 |
| **CPI 항목별 · NFP 업종별 (해부용)** | **BLS 공개 API** `api.bls.gov` | `fetch_bls.py`, finalist 날짜만. 키 불필요 |
| CPI 상대중요도(비중) | BLS 릴리스 Table 1 | **수동 고정** `data/weights/cpi_relimp.csv`, 연 1회 |
| ISM 세부지수 · NFP 개정폭 | 자동 취득 경로 없음 | **수동 입력** `data/manual/<기산일>.json` |

- **BLS 릴리스 HTML(`bls.gov/news.release/…`)은 403이다.** UA를 바꿔도 막힌다. API만 쓴다.
- ISM은 `ismworld.org` · FRED(`NAPM` 계열 중단) 모두 막혔다. 확인된 사실이니 다시 시도하지 않는다.
- 시리즈 ID는 `data/series_map.json`. **등록 전에 API 응답을 확인한다. 추측으로 넣지 않는다.**

- Investing.com **자동 스크래핑 금지** (ToS, `investpy` C&D 선례, Cloudflare). 수동 복사 후 파일로 고정.
- yfinance: **로컬 캐시 필수** (Parquet/SQLite, 증분 갱신). 벌크는 배치+딜레이. 수정종가·원본종가 둘 다 저장.
- 티커: `^GSPC` `^IXIC` `^NDX` `^SOX` `^KS11` `^KQ11`, 한국 종목은 `.KS`/`.KQ`.
- **업종은 ETF로 본다.** 미국은 SPDR 섹터 ETF(XLK, XLF, XLE…) · SOXX · SMH, 한국은 KODEX/TIGER 섹터 ETF(`.KS`). yfinance에 KRX 업종지수는 없다.
- **생존편향**: 상장폐지 종목은 yfinance에서 사라진다. 업종 분석은 지수/ETF로 우회. 개별종목은 한계를 결과에 명시.

---

## 4. 이벤트 테이블 스키마

원본 복사본은 **그대로 보존**하고, 정제는 사본에서.

`release_date` · `indicator` · `variant`(MoM/YoY × headline/core) · `actual` · `forecast` · `previous` · `unit`(%/K/index) · `pulled_on` · `notes`

**파생**: `surprise = (actual − forecast).round(2)`

> **반올림은 선택이 아니다.** `0.6 − 0.2 = 0.19999999999999996` 이라 `>= 0.20` 비교에서 탈락한다.
> 2026-09-08에 이 버그로 **조건 1 통과가 6건으로 나오고 있었다. 실제로는 8건이다.**
> 원본이 소수 1자리(%)·정수(K)이므로 2자리 반올림이면 충분하다. 최종 사례 2건은 바뀌지 않았다.

- **forecast 공란을 0으로 채우지 않는다.** NaN으로 두고 `notes`에 표시.
- **단위별 임계값 분리**: "0.2%p"는 `%` 지표(CPI, PPI, PCE, 임금, 소매판매 MoM)에만. NFP·JOLTS는 K, ISM은 지수 포인트.
- `variant` 필수. MoM/YoY, headline/core 혼동이 가장 흔한 오류.
- 과거 행은 **재조회하지 않는다.** 한 번 확정하면 고정 (재현성).

**개정판 함정 — 발표 당시 값 ≠ 지금 조회한 값.** BLS는 매년 1월 직전 5년치 계절조정계수를 다시 계산한다. 그래서 API는 *오늘 기준으로 재계산된 과거*를 준다. 실측 예 (2020-07):

| | 발표 당시 | 오늘 API |
|---|---|---|
| 휘발유 전월비 | +5.60% | +3.87% |
| 헤드라인 CPI 전월비 | +0.60% | +0.51% |
| NFP 총 증감 | +1,763K | +1,584K |

→ **서프라이즈 · 컨센서스 · "예상보다" 류는 `data/events`(발표 당시 값)에서만 쓴다.**
→ 구성 항목 표는 API 값으로 만들되 `src`에 개정판임을 남기고, 헤드라인과 어긋나는 몫은 **잔차 행**으로 흡수한다.
→ **둘을 한 문장에 섞지 않는다.**
→ NFP 개정폭은 발표 당시 vintage라야 나오므로 API로는 불가 — `data/manual`에서만 온다.
- **참조월(`ref_month`)을 반드시 남긴다.** 발표일이 다른 지표를 묶는 유일한 키다. 표기가 `(Jul)`뿐이면 발표월 기준으로 연도를 채운다 (1월 발표의 `(Dec)`는 전년).
- 규모: 월간 지표 1개당 **99행 (2018-04 ~ 2026-08)**. 출처 페이지가 1회 로드당 100건만 실어 보내는 데 따른 상한이며, 이보다 길게 가려면 별도 수단이 필요하다.

---

## 5. 통계 가드레일

**항상 함께 출력**
1. **n** — 퍼널 각 단계까지 (`전체 98 → 조건 1: 6 → 조건 2: 2 → 조건 3: 2`). 보고서는 A/B/C 대신 **조건 1 · 2 · 3**으로 부른다
2. **개별 사례 전부 나열** — n ≤ 20이면 필수. n이 작으면 통계량보다 원본 사례가 정직하다.
3. **무조건부 베이스라인 병기** — 나스닥 무조건부 5일 상승확률(≈56%)을 옆에 놓지 않으면 "58%"는 의미가 없다.

**경고를 붙일 상황**
- `n < 10` → "통계적 유의성 없음, 사례 참고용" 명시
- 이벤트 간격 < 20일 → 20일 포워드 수익률이 겹침. 자기상관, **유효 n이 명목보다 작음**
- 자연어 UI는 데이터 마이닝이 쉽다 → 베이스라인 병기가 1차 방어선

**룩어헤드 금지** — 레짐 태깅(Fed 사이클, VIX 버킷, 인플레 레벨)은 당시 관측 가능한 정보만. NBER 경기전환일은 사후 발표라 그대로 쓰면 미래 정보가 샌다.

**시장 환경이 다른 사례는 별도 구분** (원 요구사항). 레짐 태그를 붙여 필터하거나 분리 표시.

**가드레일이 보고서에 나타나는 자리** — 별도 고지 슬라이드는 두지 않는다 (6.2).
- 퍼널 n → **1장 메인 표 바로 위 한 줄**
- 베이스라인 · `n < 10` 경고 → **1장 메인 표 각주**
- 개별 사례 전부 나열 → **사례별 슬라이드 그 자체**
- 컨센서스 출처 · 룩어헤드 처리 → **1장 조건 정의 표 아래 단락**

압축해서 싣되 **빼지는 않는다.** 자리가 없으면 문장을 줄이지 표본 한계를 감추지 않는다.

---

## 6. 동작 규격

### 6.1 자연어 → 구조화 필터
LLM의 역할은 **질문을 조건으로 번역하는 것까지**다. 통계 계산은 Python이 한다.

```json
{
  "event": {"indicator": "US_CPI", "variant": "headline_MoM",
            "surprise_pp": {"op": ">=", "value": 0.2}},
  "conditions": [{"series": "DGS10", "metric": "chg_bp", "op": ">=", "value": 10, "window": "same_day"}],
  "targets": ["^IXIC", "^SOX"],
  "horizons": [1, 5, 20]
}
```

**번역된 조건을 먼저 보여주고 확인받는다.** 조용히 해석하지 않는다 (1.1).

### 6.2 보고서 포맷

**출력물은 `.pptx`.** 16:9 와이드. 사내 회람 · 편집이 되어야 하므로 PDF가 아니라 편집 가능한 슬라이드로 낸다.

> **세로 A4를 버리는 이유.** 대상 6종 × 구간 6열 표는 A4 세로 폭에 물리적으로 안 들어간다. 억지로 넣으니 본문이 7.4pt까지 내려갔고, 그 순간 읽히지 않게 됐다. 같은 표가 가로 캔버스에서는 10pt로 들어간다. **가독성 문제의 절반은 조판 실력이 아니라 폭이었다.**

**생성 스택**: python-pptx + matplotlib(PNG 삽입). Jinja2 · Playwright · WeasyPrint는 **쓰지 않는다.**

**python-pptx의 기본값은 전부 버린다.** 그대로 쓰면 파워포인트 기본 템플릿 냄새가 나고 한글이 깨진다. 함정 4개:

1. **빈 레이아웃에서 시작한다.** `prs.slide_layouts[6]`(Blank). 플레이스홀더를 쓰지 않고 `add_textbox` · `add_table` · `add_picture`로 직접 배치한다.
2. **표에 기본 테이블 스타일(파란 줄무늬)이 자동으로 붙는다.** `tbl.first_row = True`, `tbl.horz_banding = False`로 끄고 셀 배경을 직접 칠한다.
3. **한글은 `run.font.name`만으로 안 붙는다.** `rPr`에 `<a:ea typeface="맑은 고딕"/>`를 직접 넣어야 동아시아 글리프에 적용된다. 안 넣으면 PowerPoint가 임의 폰트로 대체한다.
4. **셀 서식은 셀 단위다.** 표 하나에 스타일을 한 번에 먹일 방법이 없다 → `set_cell(tbl, r, c, text, size, bold, align, color, fill)` 헬퍼를 하나 만들고 **모든 셀이 이 함수를 거치게** 한다. 셀마다 서식 코드를 늘어놓지 않는다.

> 위 4개를 처리한 구현이 [`scripts/pptx_kit.py`](scripts/pptx_kit.py) 에 있다. 색 · 폰트 크기 · 표 규칙이 전부 여기 상수로 박혀 있으므로, 새 보고서는 여기서 출발한다. 조판은 [`scripts/report.py`](scripts/report.py) 가 한다.

**슬라이드 구성은 `1 + 2N`** (N = 조건 충족 사례 수). 1장 = 조건과 사례 목록, 사례당 **2장**(지표 해부 / 시장 반응).

> **사례당 1장이 아닌 이유.** 지표 배경 · 당일 반응 · 이후 경로 · 차트 2개를 한 면에 넣으면 정보 블록이 5개가 된다. 그게 지금 보고서가 안 읽히는 직접적 원인이다. **슬라이드는 싸다. 밀도를 낮추고 장수를 늘린다.**
> 사례가 8건을 넘으면(17장 초과) 이 포맷을 쓰지 않는다. 그때는 분포 통계가 본문이 되므로 별도 협의.

---

#### 디자인 규격 — 재량 아님

캔버스 **13.333 × 7.5 in**. 좌우 여백 0.5 in, 상단 타이틀 밴드 0.9 in, 하단 푸터 0.3 in → 본문 영역 12.333 × 6.3 in.

**정보 블록은 슬라이드당 최대 3개.** 블록 = 표 1개 또는 차트 1개. 4개째가 필요하면 슬라이드를 나눈다.

| 요소 | 크기 | 색 |
|---|---|---|
| 슬라이드 제목 | 20pt Bold | 먹 |
| 소제목 (`▶`) | 13pt Bold | 남색 |
| 본문 불릿 | 11pt | 먹 |
| 표 헤더 | 10pt Bold | 남색 / 헤더배경 |
| 표 본문 | 10pt | 먹 (수치는 부호색) |
| 각주 · 자료 출처 | 8pt | 회색 |

**표 9pt · 본문 10pt 아래로 내려가지 않는다.** 안 들어가면 **열이나 행을 줄이지 글자를 줄이지 않는다.**

**색은 아래 7개뿐이다.** 이 밖의 색을 쓰지 않는다.

```
먹        #1A1A1A   본문
회색      #6B7280   보조 · 각주
남색      #1F4E79   제목 · 표 헤더 · 기준선
헤더배경   #EEF2F7
상승      #C0392B   (+) 수치      ← 국내 관행: 상승 적색 / 하락 청색
하락      #1F6FB2   (−) 수치
경고배경   #FDF3F2
```

차트 계열색도 이 안에서 쓰되, 계열이 4개를 넘으면 **명도 단계**로 구분한다. 무지개 팔레트 금지.

**표 규칙**
- **세로 괘선 없음.** 헤더 하단 1.25pt 남색 선 + 행 구분선 0.5pt `#DDD`만.
- 숫자는 우측 정렬. **소수 자릿수를 열 안에서 통일**한다 — 등폭 한글 폰트가 없으므로 자릿수 통일이 유일한 정렬 수단이다.
- 부호는 **글자색**으로 표현한다. 셀 배경 음영으로 크기를 나타내지 않는다 (인쇄 · 빔프로젝터에서 뭉갠다).
- 셀 상하 여백 0.06 in 이상.

**차트**
- matplotlib. `figsize` 가로 **5.8 in 이상**, dpi 200 이상. 2열 그리드에 손톱만 하게 넣지 않는다.
- 상 · 우 spine 제거. 격자는 **가로선만** alpha 0.25.
- **한글 폰트 필수**: `plt.rcParams['font.family'] = 'Malgun Gothic'`, `plt.rcParams['axes.unicode_minus'] = False`. 안 하면 한글은 □, 마이너스는 깨진다.
- 슬라이드 삽입 시 종횡비를 지킨 채 배치 상자에 맞춰 축소한다 (`pptx_kit.picture`).
- **`figsize` 는 들어갈 열 폭을 기준으로 잡는다.** 세로가 길면 상자 높이에 걸려 가로가 5.8in 아래로 줄어든다 — 실제로 한 번 걸렸다. 생성 후 `png_size` 로 최종 배치 폭을 확인할 것.
- `bbox_inches="tight"` 는 범례 높이를 더해 `figsize` 보다 세로가 커진다. 요청 높이를 그만큼 낮게 잡는다.

---

#### 1장 — 조건과 사례 목록

> 이 장이 보고서의 결론이다. 읽는 사람이 알고 싶은 건 **"언제 이 조건이 충족됐고, 그때 각 자산군이 얼마나 움직였나"** 둘뿐이다. 그 둘이 표에서 바로 안 보이면 실패한 장이다.

```
좌 45%                                우 55%
▶ 조건 정의 (표)                      ▶ 조건 충족일 (메인 표)
  조건 | 지표 | variant | 임계값 | 역할     표 위 한 줄: 퍼널 n
  표 아래 2~3줄:                          기산일 | 조건1 | 조건2 | 조건3 |
   분석 대상 · 관찰 구간 ·                 대상 | 당일 | 1일 | 5일 | 20일 | 60일 | 60MDD
   결합 방식(룩어헤드 처리) ·              각주 2줄: 베이스라인 중앙값 /
   컨센서스 출처                                  n<10이면 "통계적 유의성 없음"
```

**메인 표의 대상은 지수 4종으로 제한한다** (S&P500 · 나스닥 · SOX · 러셀2000). 경기민감 · 방어 바스켓은 여기 넣지 않고 **사례별 시장 반응 장의 차트에서 본다.** 대상이 6종이면 사례 2건만으로도 12행이 되어 읽히지 않는다.

기산일과 조건 값은 **병합 셀**로 묶어 날짜당 한 번만 보이게 한다.
**참조월은 표에 넣지 않는다.** 지표를 묶는 내부 조인 키일 뿐이고(4장), 읽는 사람에게 의미 있는 날짜는 **기산일 하나**다.

---

#### 2장 이후 (a) — 지표 해부

> **이번 개편의 핵심.** 기존 규격의 `지표당 최소 1줄`은 **폐기한다.** 읽는 사람이 알고 싶은 건 "CPI가 높았다"가 아니라 **"무엇이 CPI를 높였나"**다. 그건 산문이 아니라 **항목별 숫자**로만 답할 수 있다.

**조건에 쓰인 지표마다 해부 표를 하나씩 만든다.** 조건이 3개면 표 3개 — 슬라이드당 블록 3개 상한에 정확히 맞는다.

| 지표군 | 표 열 | 최소 행 |
|---|---|---|
| CPI · PCE · PPI | 항목 \| 전월비(%) \| 가중치(%) \| 기여도(%p) | 상위 기여 3개 + 상쇄 항목 1개 + 합계 |
| NFP · 고용 | 업종 \| 당월(K) \| 전월(K) | 상위 증가 3개 + 감소 1개, 그리고 별도 행에 **전월 · 전전월 개정폭 · 실업률 · 시간당임금 · 참가율** |
| ISM · PMI | 세부지수 \| 당월 \| 전월 \| 차이 \| 50 대비 방향 | **신규주문 · 생산 · 고용 · 공급자납기 · 재고 · 가격 전부** |
| 금리 · VIX 등 시장 조건 | 당일 변화 \| 당일 종가 \| 20일 전 대비 | — |

- **기여도 합이 헤드라인과 어긋나면 잔차 행을 남긴다.** 반올림으로 맞추지 않는다.
- 표 하단에 `자료: BLS API, CPI-U 계절조정 지수(CUSR) …` 형태로 **소스와 계산 방식**을 적는다 (규칙 B).
- 각 표 아래 **최대 2줄**로 "왜"를 붙인다. **표에 있는 숫자를 문장으로 다시 쓰지 않는다.** 직전 흐름과의 차이 · 분포상 위치 · 구성의 비대칭 등 **표를 읽어서는 안 나오는 것만** 쓴다.
- 특정 지표의 원자료를 못 구하면 **그 표만** "확보 실패"로 두고 나머지는 채운다.

#### `data/anatomy/<기산일>.json` — 모든 문장은 근거를 달고 다닌다

`note` · `day` · `path` · `read` 는 전부 **`{text, from}` 객체**다. 문자열로 쓰면 검증에서 FAIL.

```json
"note": [
  {"text": "종합 54.2를 만든 것은 신규주문·생산, 고용은 50 미만 유지",
   "from": ["facts.manual.ism.rows[신규주문].cur",
            "facts.manual.ism.rows[생산].cur",
            "facts.manual.ism.rows[고용].cur"]}
]
```

**근거 참조 문법** (루트는 `facts` · `results` · `events` 셋뿐):

```
facts.headline.us_core_cpi.surprise          발표 당시 서프라이즈
facts.components.cpi.rows[휘발유].mom         항목별 전월비 (개정판)
facts.components.nfp.extra.실업률.cur
facts.manual.ism.rows[고용].cur               수동 입력분
results.cases[2020-08-12].ret.^IXIC.5
results.baseline.^IXIC.5.median
events.us_core_cpi[2020-06].actual            99행 히스토리
```

**문장에 쓴 모든 측정치가 `from`으로 도달 가능해야 한다.** 도달 못 하면 근거를 추가하거나 **그 문장을 버린다.** 고칠 때 문장을 숫자에 맞추지, 숫자를 문장에 맞추지 않는다.

**조건이 3개면 해부 표도 3개다.** 기산일 지표(조건 1)만 파고 나머지를 생략하지 않는다. 조건 2 · 3이 충족되지 않았다면 그 날짜는 표본에 들어오지도 않았다 — **왜 그 지표들이 그 값이었는지가 사례 설명의 절반이다.**

---

#### 2장 이후 (b) — 시장 반응

```
좌 (6.0in)                          우 (6.1in)
▶ 구간 수익률 (표)                   [그림 N-1] 이벤트 전후 누적 경로
  대상 | 당일 | 1 | 5 | 20 | 60 |      −20 ~ +60거래일, 이벤트일 0에 세로선
  60일 MDD                           [그림 N-2] 대상별 구간 수익률 막대
  (지수 4종 + 경기민감 · 방어)
▶ 당일 시장 반응   불릿 3줄
▶ 이후 경로       불릿 4줄 + ▷ 해석 1줄
```

**불릿은 좌측 열 폭(6.0in · 10pt)에서 한 줄에 들어가야 한다 — 한글 약 49자.** 넘으면 문장을 자른다.

**당일 수익률 정의 주의.** CPI · NFP는 08:30 ET(장 시작 전) 발표라 **당일 종가가 첫 반응**이다. ISM은 10:00 ET(장중) 발표라 당일 종가에 발표 전 시간대가 섞인다 → 장중 발표 지표가 기산일일 때는 각주로 명시한다.

각 그림 하단에 `자료: Investing.com, FRED, Yahoo Finance, 자체 계산`.

---

#### 문체 규칙

- **개조식 · 음슴체 종결** (`~마감`, `~기록`, `~판단`, `~권고`). 완결형 문장 쓰지 않음
- 숫자는 소수점까지 구체적으로, 단위 명시 (`1,078.25pt`, `5.75% 급락`, `+0.34%p`)
- **해석은 슬라이드당 최대 1줄.** `▷`로 시작해 사실 불릿(`·`)과 **시각적으로 분리**한다. 나머지는 전부 사실 · 숫자 · 출처
- **기사에서 가져온 사실과 우리 계산을 섞지 않는다.** 기사 근거에는 출처를, 계산 결과에는 단위를 붙여 구분
- **불릿 1개 = 1줄.** 줄바꿈되는 불릿은 자른다. 못 자르면 표로 옮긴다

기사 인용은 **지표 해부 · 시장 반응 장에서만** 사용 (규칙 B).

---

#### 검증 ① 해석 — `py -3 scripts/check_anatomy.py`

**"기사가 그렇게 말했나"가 아니라 "이 문장을 숫자에서 다시 만들 수 있나"를 묻는다.**

1. `note` · `day` · `path` · `read` 가 `{text, from}` 객체인가
2. **모든 `from` 참조가 해석되는가** — 없는 항목을 가리키면 FAIL
3. **문장의 측정치가 근거에 존재하는가** — 근거에 없는 숫자를 쓰면 FAIL
4. 한 줄 49자 이하 · 완결형 종결(`~다`) 없음
5. `note` 블록당 ≤2줄 · `day` 3줄 · `path` 4줄 · `read` 1줄
6. **기여도 합 = 합계 ± 잔차**
7. 블록 수 = 조건 수 · 모든 블록에 `src`

**3번 검사가 인정하는 것은 `from`에 적은 값뿐이다.** 다음 셋은 **일부러 막았다.**

- **근거는 숫자 하나를 가리켜야 한다.** 객체를 통째로 짚으면 FAIL.
  `rank.cpi_surp`(= `{rank:3, n:98, pct:3.1}`)로는 안 되고 `rank.cpi_surp.rank`처럼 잎까지 짚는다.
  통째로 짚으면 안의 숫자가 전부 인정돼 **자리만 바꾼 "3개월 중 98위"가 통과한다.**
- **같은 표의 숫자를 자동으로 인정하지 않는다.** 표 값을 인용하려면 그 항목을 `from`에 적는다.
  CPI 표는 전부 작은 소수라, `+0.51`을 정수 자리로 반올림하면 `1`이 되어 **"기여 1위" 같은 날조가 통과했다.**
- **두 값의 차이도 인정하지 않는다.** 표 숫자 20개면 차이 조합이 400개가 되어 작은 정수를 거의 다 덮는다.
  차이를 쓰려면 그 값을 담은 필드를 근거로 대거나 문장을 바꾼다.

대조는 **글에 쓴 자릿수 기준**이다. `3.1`이라 썼으면 소수 1자리로 반올림해 비교한다. 고정 허용오차를 쓰면 `0.5`가 `+0.51`에 걸려 통과한다.

`3분의 1` 같은 맨숫자는 검사에서 빠진다. 부호·소수점·단위(`%` `%p` `K` `개` `개월` `위` `건`)가 붙은 것만 본다.
`확보 실패`는 FAIL이 아니다 — 그대로 두고 보고서에 표기된다.

#### 이 검사가 못 잡는 것 — 딱 하나

**한 문장 안에서 인용한 값끼리 자리를 바꾼 경우.**

```
근원 서프 +0.40%p — 98개월 중 3위, 상위 3.1%     ← 맞음
근원 서프 +0.40%p — 3개월 중 98위, 상위 3.1%     ← 통과해 버림
```

3과 98을 그 문장이 정당하게 인용했으므로, "이 숫자가 실재하나"만 묻는 검사로는 원리적으로 못 잡는다.

**다른 지표의 값을 끌어다 쓰는 건 잡힌다** — 근거에 적지 않았으면 풀에 없기 때문이다.
ISM 순위(34위)를 CPI 순위 자리에 넣으면 `근거에 없는 숫자 34`로 FAIL 난다.

→ **자리바꿈만 사람이 읽어서 본다.** 그 외 날조는 전부 스크립트가 막는다.

#### 검증 ② 조판

생성 후 스크립트로 다시 열어 확인한다:

1. 슬라이드 수 = `1 + 2N`
2. **표 셀 run ≥ 9pt · 그 외 run ≥ 8pt** — 하나라도 미달이면 FAIL
3. **모든 도형이 캔버스 안** — `left ≥ 0`, `left + width ≤ 13.333 in`, 세로도 동일
4. **모든 run에 `ea` typeface 지정됨** (한글 대체폰트 방지)

**텍스트 검사만으로는 조판 깨짐을 못 잡는다.** pywin32로 PowerPoint를 띄워 슬라이드를 PNG로 내보내고 **눈으로 한 번 본다.**

```python
app = win32com.client.Dispatch("PowerPoint.Application")
pres = app.Presentations.Open(path, WithWindow=False)
pres.Export(outdir, "PNG")
```

LibreOffice는 이 PC에 없다. PowerPoint(Office16)는 설치돼 있다.

### 6.3 개발 환경 (Windows)

- **Python 실행은 `py -3`.** `python.exe`는 Windows 스토어 스텁이라 동작하지 않는다.
- **현재 3.14.7** (`pythoncore-3.14-64`). `py -0p`에 등록된 버전은 이것 하나뿐이다 — 기존 3.9.13(Visual Studio 번들)은 사라졌다. `scripts/__pycache__`에 남은 `cpython-39.pyc`는 잔재다.
- 설치된 스크립트는 PATH에 없다 → `py -3 -m ...` 형태로 모듈 호출.
- **현재 설치됨** (2026-09-08 확인): matplotlib, pandas, numpy, yfinance, `python-pptx`, `pywin32`, `pyarrow`, `fredapi` — 전부 들어와 있다. 파이프라인이 끝까지 돈다.
- BLS API는 **키도 라이브러리도 필요 없다** (`urllib` + 공개 엔드포인트). v1은 IP당 하루 25쿼리 · 1회 25시리즈 · 10년 구간 제한.
- **FRED 키는 아직 없다** (`.env` 부재). 금리 · VIX 조건이 들어오면 거기서 멈추고 키를 요청한다.
- **다시 깔지 않는다**: `jinja2` `playwright` `PyMuPDF` — PDF 스택 잔재. 6.2에서 폐기했다.
- 한글 폰트: Malgun Gothic 설치 확인됨 (`malgun.ttf` · `malgunbd.ttf` · `malgunsl.ttf`).
- 패키지 설치가 실패하면 C++ 빌드 툴을 깔지 말고 **휠이 있는 하위 버전을 핀**한다.
- FRED API 키는 `.env`. 커밋 금지.

### 6.4 초기 이벤트 유니버스
망라하지 않는다. 시장을 실제로 움직이는 미국 지표부터:
`CPI` `Core CPI` `PCE` `Core PCE` `NFP` `실업률` `시간당임금` `ISM 제조` `ISM 서비스` `소매판매` `PPI` `GDP` `JOLTS` `신규실업수당` `FOMC`

2군 지표(지역 연은 서베이 등)는 forecast 커버리지가 얇다 → 제외하거나 `notes`로 관리.

### 6.5 실행 순서

```
① 조건을 구조로 번역 → 사용자 확인          ← 멈춤. 조용히 해석하지 않는다 (6.1)
② 데이터 보유 확인
     조건 지표가 data/events 에 없음  → 멈춤. Investing.com 수동 복사 요청
     FRED 키 필요                      → 멈춤. 키 요청
     가격 없음                         → prices.py 가 알아서 받음
③ py -3 scripts/screen.py       퍼널 · 룩어헤드 검사        [규칙 A]
     n = 0 또는 n > 8 이면 멈추고 협의
④ py -3 scripts/analyze.py      results.json
   py -3 scripts/figures.py     차트
━━━━━━━━━━━━ finalist 확정. 여기부터 규칙 B ━━━━━━━━━━━━
⑤ py -3 scripts/fetch_bls.py    data/facts/<날짜>.json
     ISM 세부 · NFP 개정폭은 data/manual/<날짜>.json 수동   ← 멈춤(선택)
⑥ interpret 에이전트            data/anatomy/<날짜>.json
⑦ py -3 scripts/check_anatomy.py   PASS 될 때까지 ⑥ 반복
⑧ py -3 scripts/report.py       pptx + 조판 검증 + PNG 렌더 → 눈으로 확인
```

**사용자에게 물어보는 곳은 ①②⑤ 세 군데뿐이다.** 나머지는 자동으로 끝까지 간다.
③에서 n이 0이거나 8을 넘으면 한 번 더 멈춘다.
