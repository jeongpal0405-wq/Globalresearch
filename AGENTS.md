# AGENTS.md

## 0. 프로젝트

**과거 유사국면 · 이벤트 스터디 도구.**
자연어 질문 → 조건에 맞는 과거 사례 탐색 → 이벤트 이후 지수 · 업종 · 종목 성과 통계 자동 산출.

> 예: "미국 CPI가 예상치를 0.2%p 이상 상회하고 10년물 금리가 10bp 이상 오른 날, 나스닥과 반도체지수의 이후 성과는?"

산출물: 조건 부합일 목록 + 발표일 종가 기준 1·5·20·60거래일 포워드 수익률 · 최대낙폭 + 사례별 차트 + 관측 데이터 기반 자체 해석 코멘트.

---

## 1. 코딩 행동 지침

> 속도보다 신중함으로 기운다. 사소한 작업엔 판단력을 발휘할 것.

**1.1 코딩 전에 생각하기** — 가정하지 말 것. 혼란을 숨기지 말 것.
- 가정을 명시한다. 불확실하면 묻는다.
- 해석이 여러 개면 모두 제시한다. 조용히 하나를 고르지 않는다.
- 더 단순한 방법이 있으면 말한다. 필요하면 반대한다.
- 불명확하면 멈춘다. 뭐가 헷갈리는지 이름 붙이고 묻는다.

**1.2 단순함 우선** — 문제를 푸는 최소한의 코드.
- 요청받지 않은 기능 · 추상화 · "유연성" 금지.
- 불가능한 시나리오에 대한 에러 핸들링 금지.
- 200줄을 썼는데 50줄이면 될 것 같으면 다시 쓴다.

**1.3 외과적 변경** — 꼭 필요한 것만 건드린다.
- 인접 코드 · 주석 · 포매팅을 "개선"하지 않는다. 기존 스타일을 따른다.
- 무관한 데드 코드는 **언급만** 한다. 지우지 않는다.
- 단, 내 변경 때문에 안 쓰이게 된 import · 변수는 제거한다.
- 테스트: 변경된 모든 줄이 사용자 요청으로 직접 추적되어야 한다.

**1.4 목표 주도 실행** — 성공 기준을 정의하고 검증될 때까지 반복한다.
- "버그 수정" → "버그를 재현하는 테스트를 쓰고 통과시킨다"
- 다단계 작업은 `단계 → 검증 방법` 형태로 계획을 먼저 진술한다.

---

## 2. 도메인 불변 규칙 — 재논의 금지

### 규칙 A. 표본은 **캘린더 전체**에서 온다. 검색어가 고르지 않는다. ★핵심★

```
❌ "CPI beat expectations" 검색 → 나온 날짜들을 분석 대상으로 삼음
✅ Investing.com 지표별 히스토리 표를 통째로 가져옴 (상회·부합·하회 전부)
   → surprise = actual − forecast 직접 계산 → 임계값 필터
```

**왜.** 뉴스는 시장이 크게 반응한 날을 크게 보도한다. 보도 강도가 **우리가 측정하려는 결과(시장 충격)와 상관**되므로, 검색으로 표본을 고르면 "상회했지만 시장이 무시한 날"이 체계적으로 빠지고 이벤트 효과가 **과대추정**된다. 또 부합일·하회일이 없으면 베이스라인이 없어 "상승확률 58%"가 높은지 낮은지 말할 수 없다.

실무적으로도, `"≥0.2%p 상회"`를 뜻하는 검색어는 **존재하지 않는다.** 기사는 "0.4%, 예상 0.3%"라고 쓰지 "0.1%p 상회"라고 쓰지 않는다. 서프라이즈 폭은 **계산으로만** 나온다.

### 규칙 B. 지정 출처의 데이터로 분석하고, 매체 코멘트는 사용하지 않는다

- 표본 생성 · 조건 판정 · 수익률 계산 · 자체 해석의 근거 데이터는 **3장의 출처 우선순위**를 따른다. 숫자 교차검증에도 같은 원칙을 적용한다.
- 컨센서스가 필요한 데이터는 **Investing.com**, 필요하지 않은 데이터는 **FRED API 우선, 이후 Investing.com**을 사용한다.
- 매체 기사나 검색 결과로 후보 날짜를 고르지 않는다. 지정 출처에서 확보한 전체 분석 대상 이력에 조건을 적용한다.

**매체 기사 · 검색 스니펫 · 전문가 코멘트를 보고서에 인용하거나 요약하지 않는다.**
- 코멘트는 Codex가 이벤트 수치, 세부지표, 대상 자산 수익률, 금리 · VIX · 달러 · 업종 간 상대성과 등 관측 데이터로 직접 작성한다.
- **관찰과 해석을 분리**한다. 확인된 움직임을 먼저 쓰고, 원인에 관한 문장은 `~로 판단` · `~로 해석`처럼 자체 해석임을 명시한다.
- 데이터만으로 인과관계를 확정하지 않는다. 근거가 부족하면 **"관측 데이터만으로 원인 특정 불가"**라고 적고 추측으로 채우지 않는다.
- **전망 · 목표가 · 투자의견은 작성하지 않는다.** 우리 보고서는 과거 사례 기술이지 전망물이 아니다.
- **조건에 쓰인 지표는 전부 해석 대상이다.** 기산일 지표만 보고 끝내지 않는다 (6.2).

### 규칙 C. 시장 변수 조건은 시계열에서 조인
"10년물 +10bp", VIX, 달러인덱스 같은 조건은 **FRED API 우선, 이후 Investing.com**에서 확보한 시계열을 사용한다. 조건 판정 시점과 시계열을 연결하는 시간 기준은 **사용자에게 질문해 확인한 뒤 적용**한다. 날짜만 같다는 이유로 당시 미공개인 값을 연결하지 않는다. 뉴스로 찾지 않는다.

### 규칙 D. 복합조건은 퍼널
`A 만족일 → 그중 B 만족 → 그중 C 만족`. 정상적인 방식이다. 단 **A가 완전해야** 의미가 있다 (규칙 A). A가 편향되면 뒤에서 뭘 해도 안 고쳐진다.

---

## 3. 데이터 소스

| 데이터 구분 | 소스 우선순위 | 방식 |
|---|---|---|
| **컨센서스가 필요한 데이터** (예상 대비 상회·하회 등) | **Investing.com** | 브라우저로 해당 지표의 히스토리와 실제치·예상치를 직접 확인하고 원본 보존 |
| **컨센서스가 필요하지 않은 데이터** (지표 수준·변화, 금리, VIX, 자산 가격 등) | **1순위 FRED API → 2순위 Investing.com** | FRED API에서 먼저 확인하고, 적합한 자료를 확보하지 못하면 Investing.com을 브라우저로 조회 |

- Investing.com은 **브라우저로 페이지를 열어 표시된 데이터를 직접 확인·복사**한다. 직접 HTTP 요청이나 비공식 API를 통한 자동 스크래핑으로 대체하지 않는다.
- 조회한 페이지 주소 또는 API 시리즈 식별자, 조회일, 수집 기간, 단위와 원본 파일을 남긴다. 브라우저로 확인하지 않은 자료를 확인했다고 쓰지 않는다.
- FRED API 우선은 **요청한 지표·자산, 기간, 빈도, 단위와 발표 당시 값 요건이 맞는 데이터**를 우선한다는 뜻이다. 비슷한 지표나 대체 자산을 임의로 선택하지 않는다. Investing.com으로 전환하면 이유를 기록한다.
- 두 출처에서 필요한 자료를 확보하지 못하면 부족한 항목을 사용자에게 알린다. 다른 출처를 임의로 추가하지 않는다.
- **로컬 캐시 필수** (Parquet/SQLite 등). 원본은 보존하고 정제는 사본에서 한다. 자산 가격의 조정 여부와 단위를 기록한다.
- 기존 yfinance/Yahoo Finance 자료는 새 출처 규칙을 충족한 것으로 간주하지 않는다. 해당 자료가 설치·저장돼 있다는 이유만으로 분석에 재사용하지 않는다.
- **업종은 ETF로 본다.** 대상 ETF는 지정 출처에서 확인하고, 사용자가 특정한 자산을 다른 ETF로 임의 대체하지 않는다.
- **생존편향**: 현재 조회 가능한 종목만으로 과거 전체를 대표하지 않는다. 상장폐지·상장 이전 등으로 가격 이력이 없으면 한계를 명시한다.
- FRED API 키는 사용자 제공, `.env`에 보관하고 커밋하지 않는다.

---

## 4. 이벤트 테이블 스키마

원본 복사본은 **그대로 보존**하고, 정제는 사본에서.

`release_date` · `release_time` · `timezone` · `indicator` · `variant`(MoM/YoY × headline/core) · `actual` · `forecast` · `previous` · `unit`(%/K/index) · `ref_month` · `source_ref` · `value_as_of` · `pulled_on` · `notes`

`source_ref`는 원본 페이지 주소 또는 API 시리즈 식별자, `value_as_of`는 값이 공개된 당시의 시점을 기록한다. 조회일(`pulled_on`)과 발표 당시 값의 시점을 혼동하지 않는다.

**파생**: `surprise = actual − forecast`

- **forecast 공란을 0으로 채우지 않는다.** NaN으로 두고 `notes`에 표시.
- **단위별 임계값 분리**: "0.2%p"는 `%` 지표(CPI, PPI, PCE, 임금, 소매판매 MoM)에만. NFP·JOLTS는 K, ISM은 지수 포인트.
- `variant` 필수. MoM/YoY, headline/core 혼동이 가장 흔한 오류.
- **경제지표는 항상 해당 발표 당시 값만 사용한다.** `actual`은 해당 회차에 발표된 실제치, `forecast`는 발표 전 당시 컨센서스, `previous`는 해당 발표 시점에 제시된 전기 값이다. 이후 수정된 값으로 바꾸지 않는다.
- 발표 당시 값이라는 근거를 지정 출처에서 확인한다. 현재 히스토리 표나 최신 API 응답이라는 이유만으로 발표 당시 값이라고 간주하지 않는다. 당시 값을 확인할 수 없으면 **"발표 당시 값 확보 불가"**로 표시하고 해당 값에 의존하는 조건은 판정 불가로 처리한다. 최신 수정치로 대신 계산하지 않는다.
- 발표 당시 값으로 검증된 원본은 **그대로 고정·보존**한다. 정정이 필요하면 근거와 새 버전을 별도로 남기고 기존 원본을 덮어쓰지 않는다.
- **참조월(`ref_month`)을 남긴다.** 월간 지표를 같은 참조월로 묶을지는 사용자에게 시간 결합 기준을 확인한 뒤 결정한다. 참조월이 같아도 발표 시점이 다르면 미공개 값이 섞이지 않는지 확인한다. 표기가 `(Jul)`뿐이면 발표월 기준으로 연도를 채운다 (1월 발표의 `(Dec)`는 전년).
- 규모: 월간 지표 1개당 **99행 (2018-04 ~ 2026-08)**. 출처 페이지가 1회 로드당 100건만 실어 보내는 데 따른 상한이며, 이보다 길게 가려면 별도 수단이 필요하다.

---

## 5. 통계 가드레일

**숫자 표기 규칙** — 보고서 · 표 · 차트 · 코멘트에 표시하는 모든 소수값은 **소수점 첫째 자리까지 반올림**한다 (`3.43 → 3.4`, `5.75 → 5.8`, `+0.34%p → +0.3%p`). 계산은 원래 정밀도로 수행하고 표시 단계에서만 반올림한다. 표본 수(n) · 날짜 · 거래일 구간 · 티커처럼 본래 정수이거나 식별자인 값에는 불필요한 `.0`을 붙이지 않는다.

**항상 함께 출력**
1. **n** — 퍼널 각 단계까지 (`전체 98 → 조건 1: 6 → 조건 2: 2 → 조건 3: 2`). 보고서는 A/B/C 대신 **조건 1 · 2 · 3**으로 부른다
2. **개별 사례 전부 나열** — n ≤ 20이면 필수. n이 작으면 통계량보다 원본 사례가 정직하다.
3. **무조건부 베이스라인 병기** — 나스닥 무조건부 5일 상승확률(≈56%)을 옆에 놓지 않으면 "58%"는 의미가 없다.

**경고를 붙일 상황**
- `n < 10` → "통계적 유의성 없음, 사례 참고용" 명시
- 이벤트 간격 < 20일 → 20일 포워드 수익률이 겹침. 자기상관, **유효 n이 명목보다 작음**
- 자연어 UI는 데이터 마이닝이 쉽다 → 베이스라인 병기가 1차 방어선

**룩어헤드 금지** — 레짐 태깅(Fed 사이클, VIX 버킷, 인플레 레벨)은 당시 관측 가능한 정보만. NBER 경기전환일은 사후 발표라 그대로 쓰면 미래 정보가 샌다.

**조건 확인 시점은 사용자 확인 사항이다.** 각 조건의 판정 시점, 복합조건을 묶는 시점, 기산일 지표를 질문해 확인한 뒤 적용한다. 사용자가 이미 명시·확정한 내용은 다시 묻지 않는다. 기산일 종가 이후에야 알 수 있는 조건이 포함되면 그 사실을 설명하고, 조건 충족 전 반응을 사후 비교하려는 것인지 확인한다. 사후 비교로 확정되면 보고서에 명시하고, 그 조건을 기산일에 이미 알고 있었던 것처럼 해석하지 않는다.

**시장 환경이 다른 사례는 별도 구분** (원 요구사항). 레짐 태그를 붙여 필터하거나 분리 표시.

**가드레일이 보고서에 나타나는 자리** — 별도 고지 면은 두지 않는다 (6.2).
- 퍼널 n · 베이스라인 · `n < 10` 경고 → **1면 메인 테이블 각주**
- 개별 사례 전부 나열 → **사례별 면 그 자체**
- 컨센서스 출처 · 룩어헤드 처리 → **1면 조건 정의 단락**

압축해서 싣되 **빼지는 않는다.** 자리가 없으면 문장을 줄이지 표본 한계를 감추지 않는다.

---

## 6. 동작 규격

### 6.1 자연어 → 구조화 필터
Codex의 역할은 **질문을 조건으로 번역하는 것까지**다. 통계 계산은 Python이 한다.

```json
{
  "event": {"indicator": "US_CPI", "variant": "headline_MoM",
            "surprise_pp": {"op": ">=", "value": 0.2}},
  "conditions": [{"series": "DGS10", "metric": "chg_bp", "op": ">=", "value": 10, "window": "same_day"}],
  "targets": ["나스닥 종합지수", "필라델피아 반도체지수"],
  "horizons": [1, 5, 20, 60],
  "return_basis": {"anchor": "release_day_close", "day_count": "trading_days"}
}
```

**조건의 판정 기준이 명확하지 않으면 반드시 사용자에게 먼저 묻는다.** Codex가 임의로 정의하거나 기본값을 정해 분석하지 않는다 (1.1).

- 질문은 결과에 영향을 주는 모호한 부분에 한정한다. 지표 종류, 임계값, 분석 대상 기간, 조건 판정 시점, 조건 간 시간 관계, 반복 발생 처리, 기산일 지표 등이 대상이다. **발표일 종가·거래일 기준 수익률은 이미 확정된 규칙이므로 다시 묻지 않는다.**
- 예: "VIX가 30을 넘었을 때"는 초과한 모든 날인지 처음 돌파한 날인지, "물가 둔화"는 어떤 물가지표의 전월비·전년비 중 무엇인지 확인한다.
- 가능한 해석과 결과에 미치는 차이를 짧게 설명하고 사용자의 답변을 받는다. 제안한 기준은 사용자가 선택하기 전까지 확정된 조건으로 취급하지 않는다.
- 답변을 받기 전에는 해당 모호한 조건에 의존하는 표본 선정과 수익률 계산을 진행하지 않는다. 응답이 없다는 이유로 임의의 기준을 적용하지 않는다.
- 사용자가 이미 명시했거나 앞서 확정한 조건은 그대로 적용하고 중복 질문하지 않는다. 모든 조건이 명확하면 구조화된 조건 요약을 보여주고 분석을 진행한다.
- 최종 판정 기준은 분석 결과의 조건 정의에 기록한다. 데이터 부족이나 사례 수 부족을 이유로 사용자 확인 없이 조건을 바꾸거나 완화하지 않는다.

**수익률 계산 기준 — 발표일 종가 · 거래일 기준으로 고정**

- 사용자가 확정한 기산일 지표의 **발표일 종가를 t=0**으로 삼는다. h거래일 수익률은 `(t+h 거래일 종가 / 발표일 종가 - 1) × 100`으로 계산한다. 1·5·20·60일은 모두 대상 자산 시장의 거래일이며 발표일은 0일이다.
- 차트의 발표일 종가 기준 누적 수익률은 **0.0%**다. `전일 종가 → 발표일 종가` 등락은 포워드 수익률에 포함하지 않는다. 기본 수익률 표·막대 차트는 1·5·20·60거래일로 구성한다.
- 최대낙폭은 발표일 종가부터 해당 관찰 구간까지 종가 경로로 계산하고, 베이스라인도 동일한 종가·거래일 기준을 적용한다.
- 복합조건에서 어느 발표일을 기산일로 삼을지, 휴장·장 마감 후 발표·해외 시차로 대상 자산의 발표일 종가와 시간 관계가 맞지 않을 경우 어떻게 연결할지는 **사용자에게 질문해 확인**한다. 자동으로 다음 거래일이나 다른 가격으로 바꾸지 않는다.

### 6.2 보고서 포맷

**출력물은 PDF.** 증권사 리서치 보고서 형식을 따른다 (기준: 한화투자증권 주간 파생시황). A4 세로.

**생성 스택**: Jinja2 HTML 템플릿 + CSS paged media → **Playwright(Chromium) `page.pdf()`**
- WeasyPrint는 Windows에서 GTK 네이티브 의존성 때문에 설치가 불안정 → 쓰지 않는다. Playwright는 Chromium을 자체 번들.
- 설치: `pip install playwright jinja2 matplotlib && playwright install chromium`
- 면 분리는 `page-break-after: always`, 2열 그림 배치는 CSS Grid, 면 번호는 `@page` 푸터.
- `html, body { margin:0 }`을 반드시 넣는다. 기본 8px 마진이 면 높이 계산에 더해져 의도치 않은 면이 생긴다.
- 표는 `break-inside: avoid`. 표가 면 경계에서 쪼개지면 읽을 수 없다.
- 개발 중에는 같은 HTML을 브라우저로 미리보기 → PDF는 마지막 단계.

**차트**: matplotlib → PNG(150dpi 이상) 저장 후 HTML에 삽입.
- **한글 폰트 필수 설정**: `plt.rcParams['font.family'] = 'Malgun Gothic'`, `plt.rcParams['axes.unicode_minus'] = False`
- 설정 안 하면 한글은 □로, 마이너스 부호는 깨져서 나온다. 매번 확인할 것.

**검증**: 생성 후 PyMuPDF(`fitz`)로 텍스트를 추출해 한글 · 마이너스 부호가 정상인지, 면 수가 `1 + 사례 수`와 맞는지 확인한다. 텍스트 검증만으로는 조판 깨짐을 못 잡으므로 **면을 PNG로 렌더해 눈으로도 한 번 본다.**
→ 동작하는 최소 구현이 [`scripts/verify_stack.py`](scripts/verify_stack.py)에 있다 (`py -3 scripts/verify_stack.py`, 산출은 `_out/`). 보고서 템플릿은 여기서 출발할 것.

**면 구성은 사례 중심이다. 면 수는 `1 + 조건 충족 사례 수`로 가변.** 사례가 2건이면 3면, 5건이면 6면.

> 통계 요약이 아니라 **개별 사례 서술**이 본문이다. n이 작을 때 요약통계는 정직하지 않다 (Part 5).
> 사례가 20건을 넘으면 이 포맷을 쓰지 않는다. 그때는 분포 통계가 본문이 되므로 별도 협의.

**1면 — 조건과 사례 목록**

> 이 면이 보고서의 결론이다. 읽는 사람이 알고 싶은 건 **"언제 이 조건이 충족됐고, 그때 각 자산군이 얼마나 움직였나"** 둘뿐이다. 그 둘이 표에서 바로 안 보이면 실패한 면이다.

```
[작성일]
이벤트 스터디

[제목: 질의를 한 줄로]
[대상 기간: 2018-04-11 ~ 2026-08-12]

▶ 조건 정의          ← 산문 불릿이 아니라 표로 낸다
   | 조건   | 지표        | variant   | 임계값              | 역할          |
   | 조건 1 | 근원 CPI     | core MoM  | surprise ≥ +0.20%p | 수익률 기산일  |
   | 조건 2 | 비농업고용    | headline  | surprise ≥ +100K   | 판정만        |
   | 조건 3 | ISM 제조업    | headline  | actual ≥ 50.0      | 판정만        |
   표 아래 2~3줄: 분석 대상 · 발표일 종가/거래일 기준 · 사용자 확인된 조건 판정 시점/결합 방식 · 컨센서스 출처

▶ 조건 충족일        ← 이 면의 핵심. 표 하나로 전부 보이게 한다
   퍼널 n 한 줄 (전체 98 → 조건 1 통과 6 → 조건 2까지 2 → 조건 3까지 2)
   메인 테이블: 행 = 조건 충족일 × 대상, 열 = 아래 순서
     기산일 | 조건 1 값 | 조건 2 값 | 조건 3 값 | 대상 | 1일 | 5일 | 20일 | 60일 | 60일 MDD
   기산일과 조건 값은 rowspan 으로 묶어 날짜당 한 번만 보이게 한다
   각주 2줄: 무조건부 베이스라인 동일 구간 중앙값 / n < 10이면 "통계적 유의성 없음, 사례 참고용"
```

**참조월은 표에 넣지 않는다.** 지표를 묶는 내부 조인 키일 뿐이고(4장), 읽는 사람에게 의미가 있는 날짜는 **기산일 하나**다. 열을 하나 더 쓸 자리가 있으면 수익률에 쓴다.

**2면 이후 — 사례별 1면** (조건 충족일 1건당 정확히 1면, 발생 순서대로)
```
[사례 N] 2020-08-12

▶ 지표 배경          ← 조건에 쓰인 지표 "전부"에 대해 각각 단다
   조건 1 근원 CPI    : 구성 항목 기여도 · 직전 흐름과의 차이를 관측 데이터로 직접 해석
   조건 2 비농업고용   : 업종별 증감 · 전월 개정 여부를 관측 데이터로 직접 해석
   조건 3 ISM 제조업   : 신규주문 · 생산 · 고용 등 세부지수와 확장/위축 전환 여부를 직접 해석
   지표당 최소 1줄. 매체 코멘트 없이 수치와 자체 해석만 기재 (규칙 B)
▶ 발표 이후 시장 반응
   발표일 종가부터 이후 1거래일 종가까지 수익률을 대상별로 제시. 같은 관찰 구간의 금리 · VIX · 달러 · 업종 간 상대성과 등을 근거로 시장 반응을 자체 해석
▶ 이후 경로
   1 · 5 · 20 · 60일 수익률, 60일 최대낙폭. 방향이 바뀐 지점이 있으면 그 시점을 짚는다
▶ 차트 (2열)
   [그림 N-1] 이벤트 전후 가격 경로 — −20 ~ +60거래일, 발표일 종가 기준 등락률로 표시하고 t=0에 세로선 표시. 음수 거래일 구간은 사전 경로로 구분
   [그림 N-2] 대상별 발표일 종가 기준 구간 수익률 막대 (1 · 5 · 20 · 60거래일)
```

**조건이 3개면 코멘트도 3개다.** 기산일 지표(조건 1)만 쓰고 나머지를 생략하지 않는다. 조건 2 · 3이 충족되지 않았다면 그 날짜는 표본에 들어오지도 않았다 — **왜 그 지표들이 그 값이었는지가 사례 설명의 절반**이다. 특정 지표를 해석할 관측 데이터가 부족하면 그 줄에만 "관측 데이터만으로 원인 특정 불가"라고 적고 나머지는 채운다.

**발표일 종가 기준 해석 주의.** 발표부터 당일 종가까지의 반응은 이후 수익률에 포함되지 않는다. 발표일 종가 이후 성과를 발표 직후의 즉각적인 반응으로 표현하지 않는다. 사용자에게 확인한 조건 판정 시점과 거래일 연결 기준을 보고서에 명시한다.

**그림**: 각 그림 하단에 실제 사용한 출처만 표시한다. 예: `자료: FRED, Investing.com, 자체 계산`. 사용하지 않은 출처를 관행적으로 넣지 않는다.

**문체 규칙**
- **개조식 · 음슴체 종결** (`~마감`, `~기록`, `~판단`, `~권고`). 완결형 문장 쓰지 않음
- 숫자는 소수점 첫째 자리까지 반올림하고 단위 명시 (`1,078.3pt`, `5.8% 급락`, `+0.3%p`)
- **관찰과 해석을 분리**하고, 해석은 `~로 판단` · `~로 해석`으로 명시
- 자체 해석의 근거가 된 관측값을 같은 단락에 명시하고, 매체의 주장으로 보강하지 않는다
- 단락당 4~6줄. 다른 단락 참조는 `(N단락 참조)`

**가독성이 1순위다.** 표는 면당 최대 2개(1면의 조건 정의 + 조건 충족일). 3개째가 필요하면 면을 나눈다. 본문 8pt 아래로 줄여서 우겨넣지 말고, 넘치면 사례를 다음 면으로 넘긴다.

매체 기사 인용 · 요약 · 코멘트는 보고서 어디에도 사용하지 않는다 (규칙 B).

### 6.3 개발 환경 (Windows)

- **Python 실행은 `py -3`.** `python.exe`는 Windows 스토어 스텁이라 동작하지 않는다. 현재 3.9.13 (Visual Studio 번들).
- 설치된 스크립트는 PATH에 없다 → `py -3 -m playwright ...` 형태로 모듈 호출.
- **Python 3.9는 EOL.** 일부 패키지가 3.9 휠을 제공하지 않는다. 설치 실패 시 C++ 빌드 툴을 깔지 말고 **휠이 있는 하위 버전을 핀**한다. 예: `greenlet==3.1.1` (3.2.x는 cp39 휠 없음).
- 설치 완료: playwright 1.60 + Chromium 148, matplotlib 3.9.4, jinja2, fredapi, pyarrow, yfinance 1.2.0, pandas 2.2.2, PyMuPDF.
- FRED API 키는 `.env`. 커밋 금지.

### 6.4 초기 이벤트 유니버스
망라하지 않는다. 시장을 실제로 움직이는 미국 지표부터:
`CPI` `Core CPI` `PCE` `Core PCE` `NFP` `실업률` `시간당임금` `ISM 제조` `ISM 서비스` `소매판매` `PPI` `GDP` `JOLTS` `신규실업수당` `FOMC`

2군 지표(지역 연은 서베이 등)는 forecast 커버리지가 얇다 → 제외하거나 `notes`로 관리.
